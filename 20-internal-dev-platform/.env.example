# --- Required ---

# Postgres connection string. Free options: Neon (neon.tech) or Supabase.
DATABASE_URL=

# Password for the self-service portal (single admin).
ADMIN_PASSWORD=

# --- Optional ---

# Default rate limit (requests/minute) for new API keys. Default: 60
DEFAULT_RATE_LIMIT=
